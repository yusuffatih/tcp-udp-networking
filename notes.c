#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <sys/socket.h>
#include <sys/types.h>
#include <arpa/inet.h>
#include <netdb.h>



int main() {
   
    // hmmm 
    // handshake 

    /*    
    struct sockaddr_in {
        hort int sin_family; // Address family, AF_INET (tcp or udp)
        unsigned short int sin_port; // Port number (max 65,535)
        struct in_addr sin_addr; // Internet address 
        unsigned char sin_zero[8]; // Same size as struct sockaddr
    };



    struct sockaddr {
        unsigned short sa_family; // address family, AF_xxx
        char sa_data[14]; // 14 bytes of protocol address
    }


    struct in_addr {
        uint32_t s_addr; // unsigned 4 byte
    };


     */
    


    return 13; // :) 
}
